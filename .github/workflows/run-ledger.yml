name: Run Ledger Disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "Run ledger workflow is disabled for the responsive tree architecture pipeline. Use Validate workflow instead."
